/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package hidrocon.core.generic.dao;

import hidrocon.core.generic.EntityManagerProvider;
import javax.persistence.EntityManager;

/**
 *
 * @author david
 */
public class HidroDAO {
	protected EntityManager entityManager;

	public HidroDAO() {
        entityManager = EntityManagerProvider.getEntityManagerProvider().getEntityManagerFactory().createEntityManager();
	}
}

/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

/*
 * NeighbourDialog.java
 *
 * Created on 06-dic-2011, 4:32:08
 */
package hidrocon.ui.neighbour.dialog;

import hidrocon.core.database.building.entities.Building;
import hidrocon.custom.dialog.HidroDialog;
import hidrocon.custom.dialog.interfaces.IEntityManager;
import hidrocon.ui.neighbour.main.NeighbourMainPanelController;
import hidrocon.ui.neighbour.main.NeighbourMainPanelModel;
import hidrocon.ui.neighbour.main.NeighbourMainPanelView;
import java.awt.Dimension;

/**
 *
 * @author David Sáenz Tagarro
 */
public class NeighbourDialog extends HidroDialog implements IEntityManager {


    private NeighbourMainPanelModel model;    
    private NeighbourMainPanelView view;
    private NeighbourMainPanelController controller;

    /** Creates new form NeighbourDialog */
    public NeighbourDialog(java.awt.Frame parent, boolean modal) {
        super(parent, modal);
        initContent();
    }
    
    private void initContent() {
        view = new NeighbourMainPanelView();
        model = new NeighbourMainPanelModel();
        controller = new NeighbourMainPanelController(model, view);
        
        controller.setDialog(this);

        view.setMinimumSize(new Dimension(500,575));
        view.setMaximumSize(new Dimension(500,575));
        view.setPreferredSize(new Dimension(500,575));
        getContentPane().add(view);
        
        //HidroBlockingLayerUI layerUI = new HidroBlockingLayerUI();
        //JLayer<JPanel> jlayerMain = new JLayer<JPanel>(view, layerUI);
        //getContentPane().add(jlayerMain);
        pack();        
    }
    
    public void setMode(int mode) {
        //do nothing
    }

    public void setEntity(Object building) {
        controller.setBuilding((Building)building);
    }
    
    public boolean isEntityUpdated() {
        //do nothing
        return false; 
    }
    
    protected void initTopPanel(){}
    protected void initFocus(){}
    protected void initKeyBinding(){}
    protected void customizeComponents(){}    


    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setDefaultCloseOperation(javax.swing.WindowConstants.DISPOSE_ON_CLOSE);
        setName("Form"); // NOI18N

        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(getContentPane());
        getContentPane().setLayout(layout);
        layout.setHorizontalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGap(0, 400, Short.MAX_VALUE)
        );
        layout.setVerticalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGap(0, 300, Short.MAX_VALUE)
        );

        pack();
    }// </editor-fold>//GEN-END:initComponents

    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
}
